<!DOCTYPE html>
<html>
<head>
    <title>Signup Page</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding-top: 130px;
        }

        .signup-container {
            width: 350px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            text-align: center;
            padding-bottom: 55px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group input {
            width: 95%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .form-group label {
            text-align: left;
            display: block;
            margin-bottom: 5px;
        }

        .btn-container {
            margin-top: 20px;
        }

        .btn {
            padding: 10px 30px;
            background-color:#DBCC8F;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .btn-secondary {
            background-color:#42396e;
            padding-top: 10px;
            position: relative;
            top: 10px;
        }
     

    </style>
</head>
<body>
<div class="signup-container">
    <h3>Enter your registered Email for password reset</h3>
    <form action="/forgotPassword" method="post">
    <div class="form-group">
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required>
    </div>
    <div>
          <button class="btn btn-secondary" type="submit"  id="signup">Submit</button>
       </div>
       <% 
       if(typeof message!=='undefined'){
        %>
        <p style="color: rgba(217, 57, 13, 0.941);"><%=message%></p>
        <%
       }
       %>
    </form>
  


</div>
</body>
</html