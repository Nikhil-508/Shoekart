<!DOCTYPE html>
<html>
<head>
    <title>Signup Page</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding-top: 130px;
        }

        .signup-container {
            width: 350px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            text-align: center;
            padding-bottom: 55px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group input {
            width: 95%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .form-group label {
            text-align: left;
            display: block;
            margin-bottom: 5px;
        }

        .btn-container {
            margin-top: 20px;
        }

        .btn {
            padding: 10px 30px;
            background-color:#DBCC8F;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .btn-secondary {
            background-color:#42396e;
            padding-top: 10px;
            position: relative;
            top: 10px;
        }
     

    </style>
</head>
<body>
<div class="signup-container">
    <h3>Resset your password </h3>
    <form action="/resetPassword" method="post">
    <div class="form-group">
        <label for="newPassword">New password:</label>
        <input type="Password" id="newPassword" name="newPassword" required>
    </div>
    <div class="form-group">
        <label for="password">confirm password:</label>
        <input type="password" id="confirmPassword" name="confirmPassword" required>
    </div>
    <div>
        <input type="hidden" name="token" value="<%= resetToken %>">
          <button class="btn btn-secondary" type="submit"  id="resetPassword">Reset password</button>
          <a href="/login" class="btn btn-secondary">Login</a>
       </div>
       <% 
       if(typeof message!=='undefined'){
        %>
        <p style="color: rgba(217, 57, 13, 0.941);"><%=message%></p>
        <%
       }
       %>
    </form>
  


</div>
</body>
</html