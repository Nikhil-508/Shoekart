<!DOCTYPE html>
<html>
<head>
    <title>Login</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding-top: 130px;
        }

        .login-container {
            width: 350px;
            margin: 0 auto;
            background-color: #fff;
            padding: 70px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group input {
            width: 95%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .form-group label {
            text-align: left;
            display: block;
            margin-bottom: 5px;
        }

        .btn-container {
            margin-top: 20px;
        }

        .btn {
            padding: 10px 30px;
            background-color:#42396e;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }

        .btn-secondary {
            background-color:#42396e;
            padding-top: 10px;
            position: relative;
            top: 10px;
            font-size: 16px;
        }

    </style>
</head>
<body>
<div class="login-container">
    <h2>Admin Login</h2>
    <form action="/admin/login" method="post">
    <div class="form-group">
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required>
    </div>
    <div class="form-group">
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required>
    </div>
    
    <div class="btn-container">
        <button class="btn" type="submit" id="login">Login</button>
    </div>
    <div>
          <button class="btn btn-secondary" id="loginUser">Forgot password?</button>
      </div>
    </form>
 

   <% 
   if(typeof message!=='undefined'){
    %>
    <p style="color: rgb(234, 11, 11);"><%=message%></p>
    <%
   }
   %>
   
</div>
</body>
</html